
language: node_js

node_js:
  - "6.11"

cache:
  - src/node_modules

branches:
  only:
  - master
  - develop

install:
  - cd src
  - npm install
  - cd ..

deploy:
  provider: firebase
  token: $FIREBASE_DEPLOY_KEY
  project: default